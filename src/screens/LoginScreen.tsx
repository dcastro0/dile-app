import { View } from "react-native";
import { FormLogin } from "../components/FormLogin";

const LoginScreen = () => {
  return (
    <View>
      <FormLogin />
    </View>
  );
};

export { LoginScreen };
